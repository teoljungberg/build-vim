# Build vim

## Usage

The list of available commands:

```sh
% make help
```

```sh
% make install-vim
% make install-nvim
```

### Debug

Pass `DEBUG=1` to `make(1)` to see the output while building.
